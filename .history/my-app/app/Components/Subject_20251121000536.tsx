'use client'
import { useState } from "react"

export default function subject(){

const[open ,setOpen]=useState(false);


    return(<div>

    <div className="">
        <span>Subjects</span>
        <span>{open ? }</span>
    </div>
    </div>)
}