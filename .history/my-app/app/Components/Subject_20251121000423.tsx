''
import { useState } from "react"

export default function subject(){

const[open ,setOpen]=useState


    return(<div>

    <div>
        <span>Subjects</span>
        <span>{open ? }</span>
    </div>
    </div>)
}